import { FilterProfileTypePipe } from './filter-profile-type.pipe';

describe('FilterProfileTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfileTypePipe();
    expect(pipe).toBeTruthy();
  });
});
